# py4j with java learning


## init 

* init python env

```shell
python -m venv venv
source venv/bin/activate
pip install -r 
```

## running 

* build 
```shell
mvn clean pacakge 
```

* running

```shell
java  -jar target/myinstance-app.jar 

python app.py ( with env )
```